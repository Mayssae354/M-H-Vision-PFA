package com.smartvision.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JwtConfig {
    // Basic structural place holder for any extended JWT-related beans if necessary.
    // Most config is handled internally by JwtTokenProvider.
}
